apiVersion: apps/v1 

kind: Deployment 

metadata: 
  name: challenge-doyoutrustme

spec: 
  replicas: 1 
  selector: 
    matchLabels: 
      app: challenge-doyoutrustme
  template: 
    metadata: 
      labels: 
        app: challenge-doyoutrustme
    spec: 
      containers: 
      - name: challenge-doyoutrustme
        image: tcctf2021registry.azurecr.io/doyoutrustme:latest 
        imagePullPolicy: "Always"

--- 

apiVersion: v1 

kind: Service 

metadata: 
  name: challenge-doyoutrustme

spec: 
  ports: 
  - port: 80
    targetPort: 80
  selector: 
    app: challenge-doyoutrustme 

--- 


apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      rewrite ^(/doyoutrustme)$ $1/index.html permanent;
  name: challenge-doyoutrustme-ingress
  namespace: default
spec:
  tls:
  - hosts:
    - challenges.silicon-ctf.party
    secretName: tls-secret
  rules:
  - host: challenges.silicon-ctf.party
    http:
      paths:
      - backend:
          serviceName: challenge-doyoutrustme
          servicePort: 80
        path: /doyoutrustme(/|$)(.*)
        pathType: Prefix
